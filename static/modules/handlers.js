
export function keyDown(event) {
    const keyName = event.key;

    switch (keyName) {
        case 'ArrowDown':
            
            break;    
        default:
            console.log(keyName);
            break;
    }
};